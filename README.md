# simple-linux-boot-informer
Add this to your startup scripts folder (e.g. /etc/init.d/) and edit the two variables. The script will notify you when a machine has booted, and display LAN/WAN IP information as well as basic system information.
